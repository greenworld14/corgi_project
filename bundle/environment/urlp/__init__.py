"""Starter URL-parser package.

This is a stub. It reads the case file and prints a well-formed but empty
component set, which scores at the floor. Your job is to turn it into a real
WHATWG URL parser (see /app/docs/CONTRACT.md).

You may restructure this package however you like; only the command-line
contract and the JSON output shape are fixed.
"""
